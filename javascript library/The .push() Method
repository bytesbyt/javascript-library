//JavaScript built-in methods specifically called on arrays to make common tasks.

//One method, .push(), allows us to add items to the end of an array.

const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('mop the floor', 'go grocery shopping');

console.log(chores);
 

